# SiftLite libSQL example

```bash
bun install
bun run --cwd examples/libsql-basic start
```

Uses `@libsql/client` against a local file database through `libsqlAdapter`.
This is the FTS5 path, not Turso-native Tantivy FTS.
