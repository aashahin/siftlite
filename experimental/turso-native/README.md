# Turso-native experimental compiler

Internal Phase 3 architecture pressure spike.

This is **not** `@siftlite/turso` and is not stable. Upstream Turso Database
FTS still required an experimental `index_method` flag at the v1.2 review.
Remote validation is unavailable in this environment.
