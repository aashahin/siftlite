# Changesets

SiftLite uses [Changesets](https://github.com/changesets/changesets) for versioning.

Packages are **not published automatically**. Publishing is owner-gated through the Release workflow.

```bash
bun run changeset
```
